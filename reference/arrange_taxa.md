# Distribute parental taxa across taxon concepts

A data frame including chains of parental taxa for each taxon concept.

## Usage

``` r
arrange_taxa(object)
```

## Arguments

- object:

  (`taxlist`) A taxlist object including taxonomic ranks and
  parent-child relationships.
